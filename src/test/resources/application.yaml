
# Mybatis
mybatis:
  type-aliases-package: hello.itemservice.domain
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    hello:
      itemservice:
        repository:
          mybatis: debug


spring:
  profiles:
    active: test

